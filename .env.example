# Baidu Maps JavaScript API key (AK).
# Register at https://lbsyun.baidu.com/ and create a "浏览器端" (browser) application.
# Copy this file to `.env.local` and fill in your key. `.env.local` is git-ignored.
VITE_BAIDU_AK=
